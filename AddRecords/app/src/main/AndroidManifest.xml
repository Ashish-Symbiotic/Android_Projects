<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.addrecords">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPage" />
        <activity android:name=".Add_Detail" />
        <activity android:name=".Edit_Detail" />
        <activity android:name=".Show_Detail" />
        <activity android:name=".Add_Sales" />
        <activity android:name=".Edit_Sales" />
        <activity android:name=".View_Sales" />
        <activity android:name=".ShowMenu" />
        <activity android:name=".Payment_Info"></activity>
    </application>

</manifest>